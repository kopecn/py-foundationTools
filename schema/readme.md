# Schema Workflow

- uses JSONSchema --> quicktype (via npm) --> generates types
    - automated using bash script
    - see [quicktype](https://app.quicktype.io)


## Install
- quicktype via `npm install -g quicktype`