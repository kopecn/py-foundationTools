{
  "id": "http://json-schema.org/unit-spherical-small-circle-waveform",
  "$schema": "http://json-schema.org/draft-06/schema#",
  "title": "UnitSphericalSmallCircleWaveformType",
  "description": "A uniformly-sampled time series of UnitSphericalSmallCircle samples, anchored at a start timestamp and sampled at a fixed interval.",
  "type": "object",
  "required": ["smallCircles", "t0", "dt"],
  "properties": {
    "smallCircles": {
      "description": "The uniformly-sampled small-circle values, in chronological order.",
      "type": "array",
      "items": {
        "$ref": "UnitSphericalSmallCircle-schema.json"
      }
    },
    "t0": {
      "description": "The timestamp of the first sample.",
      "$ref": "PrecisionTimestamp-schema.json"
    },
    "dt": {
      "description": "The fixed interval between consecutive samples.",
      "$ref": "PrecisionTimeInterval-schema.json"
    }
  }
}
