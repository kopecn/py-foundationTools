---
plan: Fix17PresentationSchemaRuntimeContract
scope: project
status: needs-approval
last_updated: 2026-09-05
semver: 1.1.0
author: Nicholas Bergantz
---

# Fix candidate 17 — presentation schema/runtime contract

Evidence: presentation JSON Schemas declare defaults and constraints that generated Python
constructors do not materialize or validate. `PresentationMetadata.file.path` defaults to
`.` in the schema but is `None` in generated Python, creating two contracts and reintroducing
ambient-current-working-directory behavior. Minimums, maximums, and `minItems` constraints
have the same boundary ambiguity.

Minimum fix: decide where schema validation and default materialization are authoritative,
provide one explicit validated construction boundary, and make output-location resolution
deterministic. Prefer a required absolute output directory or an explicitly defined path
relative to a named source/deck location.

Do not make generated dataclass construction appear validated when it is not, silently use
the process CWD, or hand-edit generated presentation modules.

## Session note — 2026-09-05 (not executed; own execution plan)

Status held at `needs-approval`. No implementation this session; this becomes its own
scoped execution plan.

Confirmed boundary (user, 2026-09-05):

- The presentation **JSON Schema is the authoritative validation contract** and
  document-semantics definition. It is independently useful as a user-facing JSON
  validation contract — not merely metadata for the generated Python representation.
- The **generated Python models are an autogenerated representation** of that schema.
  They are not a schema-validation engine; their constructors must not be presented as
  enforcing defaults, minimums, maximums, `minItems`, or other constraints that
  Quicktype does not materialize.
- Do not modify Quicktype behavior or hand-edit generated presentation modules to make
  the Python constructors appear schema-validated.
- Define **one explicit application boundary** where presentation input is validated
  against the authoritative schema before operational behavior depends on schema
  guarantees. Runtime-affecting defaults and constraints are resolved explicitly at that
  boundary, not inferred from generated constructor behavior.
- **Output-location resolution must not fall through to the process CWD.** Require an
  explicit absolute output directory, or define resolution relative to an explicitly
  named source/deck location.
- Target contract, four clear roles:
  - Schema — authoritative validation and document semantics;
  - Generated Python model — typed Python representation of the document;
  - Application boundary — validates the input and establishes any operational/runtime
    values the application requires;
  - Output / path resolution — deterministic and explicit.
- Generated presentation modules remain generated artifacts, changed only through the
  code-generation pipeline.
